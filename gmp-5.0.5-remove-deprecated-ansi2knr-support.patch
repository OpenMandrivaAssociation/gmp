--- gmp-5.0.5/configure.in.no_ansi2knr~	2012-07-20 12:59:52.694651932 +0200
+++ gmp-5.0.5/configure.in	2012-07-20 12:59:54.335631416 +0200
@@ -65,7 +65,7 @@ dnl
 dnl  Note that there's a copy of these options in the top-level Makefile.am,
 dnl  so update there too if changing anything.
 dnl
-AM_INIT_AUTOMAKE([1.8 gnu no-dependencies $(top_builddir)/ansi2knr])
+AM_INIT_AUTOMAKE([1.8 gnu no-dependencies])
 AM_CONFIG_HEADER(config.h:config.in)
 AM_MAINTAINER_MODE
 
@@ -2243,9 +2243,6 @@ fi
 echo "      MPN_PATH=\"$path\""
 
 
-# Automake ansi2knr support.
-AM_C_PROTOTYPES
-
 CL_AS_NOEXECSTACK
 
 GMP_PROG_AR
--- gmp-5.0.5/Makefile.am.no_ansi2knr~	2012-07-20 12:59:45.151746233 +0200
+++ gmp-5.0.5/Makefile.am	2012-07-20 13:00:14.325381511 +0200
@@ -25,7 +25,7 @@
 # Makefiles in subdirectories, but here we must omit it so automake gives
 # the actual ansi2knr build rule, not "cd $(top_builddir) && make ansi2knr".
 #
-AUTOMAKE_OPTIONS = 1.8 gnu no-dependencies ansi2knr
+AUTOMAKE_OPTIONS = 1.8 gnu no-dependencies
 
 
 # Libtool -version-info for libgmp.la and libmp.la.  See "Versioning" in the
